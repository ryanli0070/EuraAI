# --- Top-level deps (logical) ---
# Web framework
fastapi==0.135.3
uvicorn==0.44.0
python-multipart==0.0.26
python-dotenv==1.2.2
pydantic==2.13.1

# Image handling (downscale whiteboard PNG before shipping to the vision API).
pillow>=10.0.0

# Socratic tutor + OCR in one shot via GPT-4o vision (no Pix2Text/torch needed).
openai>=1.50.0

# Phase 6: symbolic verification + rate limiting
sympy>=1.13.0
latex2sympy2_extended>=1.0.0
slowapi>=0.1.9

# Supabase JWT validation (HS256 by default; the [crypto] extra also covers
# asymmetric algorithms if/when the project enables JWT signing keys).
pyjwt[crypto]>=2.8.0

# Direct HTTP client for server-side Supabase admin calls (account deletion).
httpx>=0.27

# --- Pinned transitives (resolved by pip during initial install) ---
annotated-types==0.7.0
anyio==4.13.0
click==8.3.2
colorama==0.4.6
h11==0.16.0
httptools==0.7.1
idna==3.11
pydantic_core==2.46.1
PyYAML==6.0.3
starlette==1.0.0
typing-inspection==0.4.2
typing_extensions==4.15.0
watchfiles==1.1.1
websockets==16.0
